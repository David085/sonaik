using Microsoft.AspNetCore.Mvc;
using System.Xml.Linq;

namespace WebApplication1.Controllers
{
    public class FacadeController
    {
        //[HttpGet(Name = "ConsultarAutenticacion")]
        //public IEnumerable<infoQR> ConsultarAutenticacion(QR qr, Cedula int)
        //{
        //    return Info.Range(1, 5).Select(index => new WeatherForecast
        //    {
        //        Date = DateTime.Now.AddDays(index),
        //        TemperatureC = Random.Shared.Next(-20, 55),
        //        Summary = Summaries[Random.Shared.Next(Summaries.Length)]
        //    })
        //    .ToArray();
        //}

        //[HttpGet(Name = "VentaNueva")]
        //public IEnumerable<infoQR> ConsultarAutenticacion(User user, Zapato zapato)
        //{
        //    return QR.Range(1, 5).Select(index => new WeatherForecast
        //    {
        //        Date = DateTime.Now.AddDays(index),
        //        TemperatureC = Random.Shared.Next(-20, 55),
        //        Summary = Summaries[Random.Shared.Next(Summaries.Length)]
        //    })
        //    .ToArray();
        //}

        //[HttpGet(Name = "VentaVieja")]
        //public IEnumerable<infoQR> ConsultarAutenticacion(UserV user, UserC user, Zapato zapato)
        //{
        //    return QR.Range(1, 5).Select(index => new WeatherForecast
        //    {
        //        Date = DateTime.Now.AddDays(index),
        //        TemperatureC = Random.Shared.Next(-20, 55),
        //        Summary = Summaries[Random.Shared.Next(Summaries.Length)]
        //    })
        //    .ToArray();
        //}
    }
}
